{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "anyOf": [
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 384 },
        "vector": {
          "minItems": 384,
          "maxItems": 384,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 512 },
        "vector": {
          "minItems": 512,
          "maxItems": 512,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 768 },
        "vector": {
          "minItems": 768,
          "maxItems": 768,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 1024 },
        "vector": {
          "minItems": 1024,
          "maxItems": 1024,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 1536 },
        "vector": {
          "minItems": 1536,
          "maxItems": 1536,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 3072 },
        "vector": {
          "minItems": 3072,
          "maxItems": 3072,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "schemaVersion": { "type": "string", "const": "houston.dev/intelligence/v0.1" },
        "id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
        "target": {
          "type": "object",
          "properties": {
            "resourceUrn": {
              "type": "string",
              "pattern": "^urn:agents-sdk:([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9-]{0,62}):([a-z][a-z0-9._-]{1,127}):([A-Za-z0-9][A-Za-z0-9._~-]{0,127})$"
            },
            "fragmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "enrichmentId": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._~-]{0,127}$" },
            "fieldPath": { "type": "string", "pattern": "^(?:\\/(?:[^~/]|~[01])*)*$" }
          },
          "required": ["resourceUrn"],
          "additionalProperties": false
        },
        "model": { "type": "string", "minLength": 1 },
        "modelVersion": { "type": "string", "minLength": 1 },
        "contentHash": { "type": "string", "pattern": "^(?:sha256:)?[a-f0-9]{64}$" },
        "policyVersion": { "type": "string", "minLength": 1 },
        "state": {
          "type": "string",
          "enum": ["active", "superseded", "tombstoned", "purged", "error"]
        },
        "dimensions": { "type": "number", "const": 4096 },
        "vector": {
          "minItems": 4096,
          "maxItems": 4096,
          "type": "array",
          "items": { "type": "number" }
        }
      },
      "required": [
        "schemaVersion",
        "id",
        "target",
        "model",
        "modelVersion",
        "contentHash",
        "policyVersion",
        "state",
        "dimensions",
        "vector"
      ],
      "additionalProperties": false
    }
  ]
}
