# Agents SDK local Claude guidance

Follow `AGENTS.md`. This repository owns Agents SDK product code and project-local context only. Reuse global capabilities from `~/.agent-shared`, use You.md as the portable project and identity catalog, and record imported patterns plus evidence in `project-context/provenance.md`.

Do not copy secrets, private client material, paid assets, or generated project-brain artifacts into this repository. Do not represent roadmap categories as implemented inventory.
